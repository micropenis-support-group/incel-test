function prepare() {
    var S = document.getElementById("chart");
    var chart = S.getSVGDocument();
    var dot = chart.getElementById('pinpoint');
}

function moveX(xMove) {
    dot.setAttribute()
}